- name: Install packages
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: Install packages
    ansible.builtin.packages:
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - mysql
    - zip